package presenter.fragment;

import base.BasePresenterImpl;
import contract.fragment.VersionContract;

public class VersionPresenter extends BasePresenterImpl<VersionContract.View> implements VersionContract.Presenter {
}